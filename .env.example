# Copie este arquivo para .env e preencha com suas credenciais

# URL da API (pública - pode ser compartilhada)
API_BASE_URL=https://trampo40.vercel.app/api

# String de conexão do PostgreSQL (SENSÍVEL - não compartilhar)
# Para desenvolvimento local, use a string do seu banco local
# Para produção no Vercel, configure esta variável nas Environment Variables do projeto
DATABASE_URL=postgres://username:password@host:port/database?sslmode=require
